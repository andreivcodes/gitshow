/**
 * Contribution types - re-exported from shared schemas for backwards compatibility.
 *
 * The canonical source of truth is now src/lib/schemas/index.ts
 * These re-exports maintain existing import paths.
 */

export type { ContributionDay, ContributionData, ScraperConfig, Theme } from "@/lib/schemas";
